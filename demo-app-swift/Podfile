# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'CloudXSwiftRemotePods' do
  use_frameworks! :linkage => :static

  # Pods for CloudXSwiftRemotePods
  # CloudXCore from local source (for development/debugging)
  pod 'CloudXCore', :path => '../core'
  pod 'CloudXRenderer', :path => '../renderer-cloudx'
  pod 'CloudXMetaAdapter', :path => '../adapter-meta'

  target 'CloudXSwiftRemotePodsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CloudXSwiftRemotePodsUITests' do
    # Pods for testing
  end

end