platform :ios, '15.0'

target 'CloudXObjCRemotePods' do
  use_frameworks! :linkage => :static

  pod 'CloudXCore', '~> 1.3.0'
  # Use local podspec with FAN 6.21.0 dependency
  pod 'CloudXMetaAdapter', :path => '../adapter-meta'
  pod 'CloudXRenderer', '~> 1.3.0'
  pod 'CloudXVungleAdapter', '~> 1.3.0'
  pod 'CloudXInMobiAdapter', '~> 1.3.0'
  
  # Force FAN 6.21.0 to reproduce crash
  pod 'FBAudienceNetwork', '6.21.0'

  target 'CloudXObjCRemotePodsTests' do
    inherit! :search_paths
  end
  target 'CloudXObjCRemotePodsUITests' do
  end
end
