source 'https://cdn.cocoapods.org/'

# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'CloudXObjCRemotePods' do
  use_frameworks! :linkage => :static

  # Pods for CloudXObjCRemotePods
  # For REMOTE: pod 'CloudXCore'
  # For LOCAL development: just use :path, CocoaPods will find -local.podspec
  pod 'CloudXCore', :path => '../core'
  pod 'CloudXMetaAdapter', :path => '../adapter-meta'
  pod 'CloudXPrebidAdapter', :path => '../adapter-cloudx'
  # Vungle adapter temporarily removed - not configured server-side for this app
  # pod 'CloudXVungleAdapter', :path => '../adapter-vungle'

  target 'CloudXObjCRemotePodsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CloudXObjCRemotePodsUITests' do
    # Pods for testing
  end

end
